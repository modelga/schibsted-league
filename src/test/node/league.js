/* eslint-env node, mocha */
require('./config');

const { league } = require('../../main/node/db');

describe('a league test', () => {
  it('league can be created', (done) => {
    done();
  });
});
